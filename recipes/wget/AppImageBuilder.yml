version: 1
script:
  - rm -r ./$TARGET_APPDIR || true

AppDir:
  app_info:
    id: org.gnu.wget
    version: !ENV '${APP_VERSION}'
    name: wget
    icon: utilities-terminal
    exec: usr/bin/wget

  apt:
    arch: !ENV ${TARGET_ARCH}
    sources:
      - sourceline: !ENV 'deb [arch=${TARGET_ARCH}] http://archive.ubuntu.com/ubuntu/ xenial main restricted universe multiverse'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x871920d1991bc93c'

    include:
      - wget
      - libssl1.0.0
      # Domain name resolution lib
      - libnss-mdns

  files:
    exclude:
      - usr/lib/x86_64-linux-gnu/gconv
      - usr/share/man
      - usr/share/doc/*/README.*
      - usr/share/doc/*/changelog.*
      - usr/share/doc/*/NEWS.*
      - usr/share/doc/*/TODO.*

  runtime:
    env:
      PATH: '${APPDIR}/usr/bin:${APPDIR}/bin:${PATH}'

  test:
    debian:
      image: Lord-Kamina/tests-env:debian-bookworm
      command: "./AppRun --version"
    centos:
      image: Lord-Kamina/tests-env:centos-10
      command: "./AppRun --version"
    arch:
      image: Lord-Kamina/tests-env:archlinux-latest
      command: "./AppRun --version"
    fedora:
      image: Lord-Kamina/tests-env:fedora-42
      command: "./AppRun --version"
    ubuntu:
      image: Lord-Kamina/tests-env:ubuntu-noble
      command: "./AppRun --version"

AppImage:
  arch: x86_64
  update-information: None
  sign-key: None
  file_name: !ENV 'wget-${APP_VERSION}-${TARGET_ARCH}.AppImage'
