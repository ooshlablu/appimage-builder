version: 1
script:
  # Remove any previous build
  - rm -rf $TARGET_APPDIR  || true
  - mkdir -p $TARGET_APPDIR/usr/share/icons/hicolor/scalable/apps $TARGET_APPDIR/usr/bin $TARGET_APPDIR/opt "$BUILD_DIR"
  - cp ../../resources/appimage-builder.svg $TARGET_APPDIR/usr/share/icons/hicolor/scalable/apps
  - cp ../../resources/application-vnd.appimage.svg $TARGET_APPDIR/usr/share/icons/hicolor/scalable/apps

AppDir:
  app_info:
    id: org.appimage-crafters.appimage-builder
    name: appimage-builder
    icon: appimage-builder
    version: 'latest'
    # Set the python executable as entry point
    exec: usr/bin/python3
    # Set the application main script path as argument. Use '$@' to forward CLI parameters
    exec_args: "$APPDIR/usr/local/bin/appimage-builder $@"

  apt:
    arch: amd64
    sources:
      - sourceline: 'deb [arch=amd64] http://archive.ubuntu.com/ubuntu/ noble main restricted universe multiverse'
      - sourceline: 'deb [arch=amd64] http://archive.ubuntu.com/ubuntu/ noble-updates main restricted universe multiverse'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x871920d1991bc93c'

    include:
      - bash
      - coreutils
      - binutils
      - util-linux
      - file
      - desktop-file-utils
      - fakeroot
      - libgdk-pixbuf2.0-dev
      - patchelf
      - python3
      - squashfs-tools
      - strace
      - zsync
    exclude: []

  after_bundle: |
    # configure tools 
    mkdir -p $TARGET_APPDIR/bin
    ln -s fakeroot-sysv $TARGET_APPDIR/usr/bin/fakeroot
    ln -s bash $TARGET_APPDIR/bin/bash

    # install appimage-builder
    python3 -m pip install --ignore-installed --prefix=/usr/local --root="$TARGET_APPDIR" ../../
    python3 -m pip install --ignore-installed --prefix=/usr/local --root="$TARGET_APPDIR" certifi

  runtime:
    debug: true
    version: "continuous"
    env:
      # Set python home
      # See https://docs.python.org/3/using/cmdline.html#envvar-PYTHONHOME
      PYTHONHOME: '${APPDIR}/usr'
      # Path to the site-packages dir or other modules dirs
      # See https://docs.python.org/3/using/cmdline.html#envvar-PYTHONPATH
      PYTHONPATH: '${APPDIR}/usr/lib/python{{APPIMAGE_BUILDER_PYTHON_VERSION}}/site-packages'
      # SSL Certificates are placed in a different location for every system therefore we ship our own copy
      SSL_CERT_FILE: '${APPDIR}/usr/lib/python{{APPIMAGE_BUILDER_PYTHON_VERSION}}/site-packages/certifi/cacert.pem'

  test:
    fedora:
      image: Lord-Kamina/tests-env:fedora-42
      command: ./AppRun --help
    debian:
      image: Lord-Kamina/tests-env:debian-bookworm
      command: ./AppRun --help
    arch:
      image: Lord-Kamina/tests-env:archlinux-latest
      command: ./AppRun --help
    centos:
      image: Lord-Kamina/tests-env:centos-10
      command: ./AppRun --help
    ubuntu:
      image: Lord-Kamina/tests-env:ubuntu-noble
      command: ./AppRun --help

AppImage:
  file_name: "appimage-builder-{{APPIMAGE_BUILDER_VERSION}}-x86_64.AppImage"
  update-information: 'gh-releases-zsync|AppImageCrafters|appimage-builder|latest|-*x86_64.AppImage.zsync'
  sign-key: None
  arch: x86_64

